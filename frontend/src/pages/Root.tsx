import Sidenav from "../components/Sidenav";

const Root = () => {
  return (
    <div className="bg-gradient-to-t from-secondary to-primary h-screen">
      <Sidenav />
    </div>
  );
};

export default Root;
